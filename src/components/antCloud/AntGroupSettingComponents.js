import React, { PropTypes } from 'react';
import { Icon,Button,Row,Col} from 'antd';

const AntGroupSettingComponents = React.createClass({

    getInitialState() {
        return {

        };
    },

    componentDidMount(){

    },

    /**
     * 渲染页面
     * @returns {XML}
     */
    render() {

        return (
            <div>
                组织架构
            </div>
        );
    },
});

export default AntGroupSettingComponents;
